# Download Artifacts

Place generated app installers here before deploying the website:

- `Trekunity-Android.apk`
- `Trekunity-Windows-Setup.exe`

The binary files are intentionally ignored by Git because they are too large for normal repository commits.
